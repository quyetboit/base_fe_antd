import { CheckIncludePipe } from './check-include.pipe';

describe('CheckIncludePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckIncludePipe();
    expect(pipe).toBeTruthy();
  });
});
